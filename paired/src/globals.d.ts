/** The build this bundle was compiled from, stamped in by vite.config.ts and
 *  compared against /version.json at runtime (see main.tsx) to detect a stale
 *  service worker. */
declare const __BUILD_ID__: string;
